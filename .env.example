# Azure AD
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/reunioes

# Basic Auth (API)
BASIC_AUTH_USER=admin
BASIC_AUTH_PASS=changeme

# CORS (opcional)
FRONTEND_ORIGIN=http://localhost:5173

# Email (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
EMAIL_TO=

# Teams Webhook (opcional)
TEAMS_WEBHOOK_URL=

# App
PORT=3000
SYNC_CRON=0 * * * *

# Graph scoping (opcional, recomendado para app-only)
GRAPH_USER_ID=
GRAPH_LOOKBACK_HOURS=24

